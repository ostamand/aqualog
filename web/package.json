{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npx parcel src/*.html",
    "build": "npx parcel build --no-cache src/*.html; rm -r -f ../server/public; mkdir ../server/public; cp dist/* ../server/public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.7.0"
  }
}
